const CardProfile = () => {
  return <>CardProfile</>;
};
const renderSource = ``;

const iProps = ``;

const cardProfile = { ui: <CardProfile />, renderSource, iProps };

export default cardProfile;
